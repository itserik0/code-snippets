//find tickets scroll 
$('#selector').on("click",function(e){
    var position = $('#elem').position();
    position = position.top;
    
    $("html, body").animate({
        scrollTop: position 
    }, 
    500);

    e.preventDefault(); 
});
